<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmMediaFileDao">
    
    <!-- 表名 -->
    <sql id="tableName">swm_media_file</sql>
    
    <!-- 表字段 -->
    <sql id="columns">
        a.id AS "id",
        a.file_name AS "fileName",
        a.file_type AS "fileType",
        a.file_size AS "fileSize",
        a.file_url AS "fileUrl",
        a.thumbnail_url AS "thumbnailUrl",
        a.upload_by AS "uploadBy",
        a.upload_by_name AS "uploadByName",
        a.upload_time AS "uploadTime",
        a.tags AS "tags",
        a.business_type AS "businessType",
        a.business_id AS "businessId",
        a.status AS "status",
        a.remarks AS "remarks",
        a.create_by AS "createBy",
        a.create_date AS "createDate",
        a.update_by AS "updateBy",
        a.update_date AS "updateDate"
    </sql>
    
    <!-- 查询条件 -->
    <sql id="where">
        <where>
            <if test="fileName != null and fileName != ''">
                AND a.file_name LIKE #{fileName}
            </if>
            <if test="fileType != null and fileType != ''">
                AND a.file_type = #{fileType}
            </if>
            <if test="uploadBy != null and uploadBy != ''">
                AND a.upload_by = #{uploadBy}
            </if>
            <if test="tags != null and tags != ''">
                AND a.tags LIKE #{tags}
            </if>
            <if test="businessType != null and businessType != ''">
                AND a.business_type = #{businessType}
            </if>
            <if test="businessId != null and businessId != ''">
                AND a.business_id = #{businessId}
            </if>
            <if test="status != null and status != ''">
                AND a.status = #{status}
            </if>
            <if test="beginUploadTime != null and endUploadTime != null">
                AND a.upload_time BETWEEN #{beginUploadTime} AND #{endUploadTime}
            </if>
        </where>
    </sql>
    
    <!-- 根据ID获取单条数据 -->
    <select id="get" resultType="com.jeesite.modules.swm.entity.SwmMediaFile">
        SELECT <include refid="columns"/> 
        FROM <include refid="tableName"/> a
        WHERE a.id = #{id}
    </select>
    
    <!-- 查询数据列表 -->
    <select id="findList" resultType="com.jeesite.modules.swm.entity.SwmMediaFile">
        SELECT <include refid="columns"/> 
        FROM <include refid="tableName"/> a
        <include refid="where"/>
        ORDER BY a.upload_time DESC
    </select>
    
    <!-- 插入数据 -->
    <insert id="insert">
        INSERT INTO <include refid="tableName"/>(
            id,
            file_name,
            file_type,
            file_size,
            file_url,
            thumbnail_url,
            upload_by,
            upload_by_name,
            upload_time,
            tags,
            business_type,
            business_id,
            status,
            remarks,
            create_by,
            create_date,
            update_by,
            update_date
        ) VALUES (
            #{id},
            #{fileName},
            #{fileType},
            #{fileSize},
            #{fileUrl},
            #{thumbnailUrl},
            #{uploadBy},
            #{uploadByName},
            #{uploadTime},
            #{tags},
            #{businessType},
            #{businessId},
            #{status},
            #{remarks},
            #{createBy},
            #{createDate},
            #{updateBy},
            #{updateDate}
        )
    </insert>
    
    <!-- 更新数据 -->
    <update id="update">
        UPDATE <include refid="tableName"/> SET
            file_name = #{fileName},
            file_type = #{fileType},
            file_size = #{fileSize},
            file_url = #{fileUrl},
            thumbnail_url = #{thumbnailUrl},
            upload_by = #{uploadBy},
            upload_by_name = #{uploadByName},
            upload_time = #{uploadTime},
            tags = #{tags},
            business_type = #{businessType},
            business_id = #{businessId},
            status = #{status},
            remarks = #{remarks},
            update_by = #{updateBy},
            update_date = #{updateDate}
        WHERE id = #{id}
    </update>
    
    <!-- 删除数据 -->
    <update id="delete">
        UPDATE <include refid="tableName"/> SET 
            status = '1',
            update_by = #{updateBy},
            update_date = #{updateDate}
        WHERE id = #{id}
    </update>
    
    <!-- 物理删除数据 -->
    <delete id="deletePhysical">
        DELETE FROM <include refid="tableName"/>
        WHERE id = #{id}
    </delete>
    
</mapper> 