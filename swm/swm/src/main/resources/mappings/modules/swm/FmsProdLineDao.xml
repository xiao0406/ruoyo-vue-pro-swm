<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.FmsProdLineDao">
	
	<!-- 查询数据
	<select id="findList" resultType="FmsProdLine">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

    <select id="getByProdLineCode" resultType="com.jeesite.modules.fms.entity.FmsProdLine">
        SELECT * FROM fms_prod_line WHERE prod_line_code = #{prodLineCode}
    </select>
</mapper>