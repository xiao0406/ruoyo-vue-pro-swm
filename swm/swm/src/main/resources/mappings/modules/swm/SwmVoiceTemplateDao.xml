<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmVoiceTemplateDao">
	
	<!-- 查询数据 -->
	<select id="findList" resultType="SwmVoiceTemplate">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
			<if test="random != null and random != 0 ">
				AND #{random} = #{random}
			</if>
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select>
	
	<!-- 查询数据（不过滤status） -->
	<select id="findListWithoutStatusFilter" resultType="SwmVoiceTemplate">
		SELECT a.`id` AS "id",
		       a.`template_name` AS "templateName",
		       a.`template_code` AS "templateCode",
		       a.`content` AS "content",
		       a.`voice_text` AS "voiceText",
		       a.`language` AS "language",
		       a.`push_method` AS "pushMethod",
		       a.`push_frequency` AS "pushFrequency",
		       a.`status` AS "status",
		       a.`create_by` AS "createBy",
		       a.`create_date` AS "createDate",
		       a.`update_by` AS "updateBy",
		       a.`update_date` AS "updateDate",
		       a.`remarks` AS "remarks"
		FROM `swm_voice_template` a
		<where>
			<if test="templateName != null and templateName != ''">
				AND a.`template_name` LIKE CONCAT('%', #{templateName}, '%')
			</if>
			<if test="templateCode != null and templateCode != ''">
				AND a.`template_code` = #{templateCode}
			</if>
		</where>
		ORDER BY a.update_date DESC
	</select>
	
	<!-- 根据ID列表批量查询语音模板 -->
	<select id="findByIds" resultType="SwmVoiceTemplate">
		SELECT a.`id` AS "id",
		       a.`template_name` AS "templateName",
		       a.`template_code` AS "templateCode",
		       a.`content` AS "content",
		       a.`voice_text` AS "voiceText",
		       a.`language` AS "language",
		       a.`push_method` AS "pushMethod",
		       a.`push_frequency` AS "pushFrequency",
		       a.`status` AS "status",
		       a.`create_by` AS "createBy",
		       a.`create_date` AS "createDate",
		       a.`update_by` AS "updateBy",
		       a.`update_date` AS "updateDate",
		       a.`remarks` AS "remarks"
		FROM `swm_voice_template` a
		WHERE a.id IN 
		<foreach collection="ids" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
	</select>
	
	<!-- 查询状态为0的所有数据 -->
	<select id="findListWithStatusZero" resultType="SwmVoiceTemplate">
		SELECT a.`id` AS "id",
		       a.`template_name` AS "templateName",
		       a.`template_code` AS "templateCode",
		       a.`content` AS "content",
		       a.`voice_text` AS "voiceText",
		       a.`language` AS "language",
		       a.`push_method` AS "pushMethod",
		       a.`push_frequency` AS "pushFrequency",
		       a.`status` AS "status",
		       a.`create_by` AS "createBy",
		       a.`create_date` AS "createDate",
		       a.`update_by` AS "updateBy",
		       a.`update_date` AS "updateDate",
		       a.`remarks` AS "remarks"
		FROM `swm_voice_template` a
		<where>
			a.`status` = '0' and a.`template_code` IN ('1', '2')
			<if test="templateName != null and templateName != ''">
				AND a.`template_name` LIKE CONCAT('%', #{templateName}, '%')
			</if>
			<if test="templateCode != null and templateCode != ''">
				AND a.`template_code` = #{templateCode}
			</if>
		</where>
		ORDER BY a.update_date DESC
	</select>
	
	<!-- 物理删除数据 -->
	<delete id="deletePhysical">
		DELETE FROM `swm_voice_template`
		WHERE id = #{id}
	</delete>
	
	<!-- 更新状态 -->
	<update id="updateStatus">
		UPDATE `swm_voice_template`
		SET `status` = #{status}
		WHERE id = #{id}
	</update>
	
</mapper> 