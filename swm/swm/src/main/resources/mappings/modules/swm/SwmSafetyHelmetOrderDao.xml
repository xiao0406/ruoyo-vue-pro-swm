<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmSafetyHelmetOrderDao">
    
    <!-- 根据人员ID查询订购记录 -->
    <select id="findByPersonId" resultType="com.jeesite.modules.swm.entity.SwmSafetyHelmetOrder">
        SELECT 
            ${sqlMap.column.toSql()}
        FROM 
            ${sqlMap.table.toSql()}
        WHERE 
            person_id = #{personId}
        ORDER BY 
            order_date DESC
    </select>
    
    <!-- 根据安全帽ID查询订购记录 -->
    <select id="findByHelmetId" resultType="com.jeesite.modules.swm.entity.SwmSafetyHelmetOrder">
        SELECT 
            ${sqlMap.column.toSql()}
        FROM 
            ${sqlMap.table.toSql()}
        WHERE 
            helmet_id = #{helmetId}
        ORDER BY 
            order_date DESC
    </select>
    
    <!-- 更新订购状态 -->
    <update id="updateOrderStatus">
        UPDATE 
            swm_safety_helmet_order
        SET 
            order_status = #{orderStatus},
            update_by = #{updateBy},
            update_date = #{updateDate}
        <if test="orderStatus == '2' and receivedDate != null">
            , received_date = #{receivedDate}
        </if>
        <if test="receivedSign != null and receivedSign != ''">
            , received_sign = #{receivedSign}
        </if>
        WHERE 
            id = #{id}
    </update>
    
</mapper> 