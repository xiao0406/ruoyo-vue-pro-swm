<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmHelmetConfigDao">
	
	<!-- 查询数据 -->
	<select id="findList" resultType="SwmHelmetConfig">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select>
	
	<!-- 获取车间数据 -->
	<!-- author: Shawn -->
	<!-- date: 2025/06/24 -->
	<!-- description: 查询fms_position_archive表中type='CJ'的车间数据 -->
	<select id="getWorkshopData" resultType="java.util.Map">
		SELECT 
			id, 
			'0001A110000000002ZWA' AS parent_id, 
			position_name AS title 
		FROM fms_position_archive 
		WHERE type = 'CJ' 
		  AND region = '0001A110000000002ZWA' 
		  AND status = '0'
	</select>
	
</mapper> 