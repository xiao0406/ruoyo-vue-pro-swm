<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmHelmetDeviceDao">
    
    <!-- 
        注意：基本的CRUD操作已由实体类注解自动处理
        此文件中只保留特殊查询方法
    -->
    
    <!-- 查询可用的安全帽列表（未绑定人员的） -->
    <select id="findAvailableHelmets" resultType="com.jeesite.modules.swm.entity.SwmHelmetDevice">
        SELECT 
            a.id,
            a.helmet_id,
            a.helmet_type,
            a.battery_level,
            a.ip,
            a.mac_address,
            a.assigned_person,
            a.assigned_workshop,
            a.assigned_process,
            a.assigned_team,
            a.motion_status,
            a.bind_time,
            a.unbind_time,
            a.bind_duration_days,
            a.usage_status,
            a.create_by,
            a.create_date,
            a.update_by,
            a.update_date,
            a.remarks
        FROM swm_helmet_device a
        WHERE (a.assigned_person IS NULL OR a.assigned_person = '')
        <if test="keyword != null and keyword != ''">
            AND a.helmet_id LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 根据头盔编号查询设备 -->
    <select id="getByHelmetId" resultType="com.jeesite.modules.swm.entity.SwmHelmetDevice">
        SELECT 
            a.id,
            a.helmet_id,
            a.helmet_type,
            a.battery_level,
            a.ip,
            a.mac_address,
            a.assigned_person,
            a.assigned_workshop,
            a.assigned_process,
            a.assigned_team,
            a.motion_status,
            a.bind_time,
            a.unbind_time,
            a.bind_duration_days,
            a.usage_status,
            a.create_by,
            a.create_date,
            a.update_by,
            a.update_date,
            a.remarks
        FROM swm_helmet_device a
        WHERE a.helmet_id = #{helmetId}
        LIMIT 1
    </select>
    
</mapper> 