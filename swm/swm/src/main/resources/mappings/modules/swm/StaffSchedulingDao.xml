<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.StaffSchedulingDao">
    
    <!-- 根据单位ID获取人员列表 -->
    <select id="getPersonsByOffice" resultType="Map">
        SELECT 
            p.id, 
            p.name, 
            p.identity_card AS identityCard
        FROM swm_person p
        WHERE p.company = #{officeId}
        AND p.status = '0'
    </select>
    
    <!-- 根据车间ID获取人员列表 -->
    <select id="getPersonsByWorkshop" resultType="Map">
        SELECT 
            p.id, 
            p.name, 
            p.identity_card AS identityCard
        FROM swm_person p
        WHERE p.department = #{workshopId}
        AND p.status = '0'
    </select>
    
    <!-- 根据产线ID获取人员列表 -->
    <select id="getPersonsByProdLine" resultType="Map">
        SELECT 
            p.id, 
            p.name, 
            p.identity_card AS identityCard
        FROM swm_person p
        WHERE p.prod_line = #{prodLineId}
        AND p.status = '0'
    </select>
    
    <!-- 根据班组ID获取人员列表 -->
    <select id="getPersonsByWorkGroup" resultType="Map">
        SELECT 
            p.id, 
            p.name, 
            p.identity_card AS identityCard
        FROM swm_person p
        WHERE p.team = #{workGroupId}
        AND p.status = '0'
    </select>
    
</mapper>
