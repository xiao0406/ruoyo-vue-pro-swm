<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.swm.dao.SwmDifyDao">
	
	<!-- 查询数据
	<select id="findList" resultType="SwmDify">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="getEntity" resultType="com.jeesite.modules.swm.entity.SwmDify">
		SELECT * from swm_dify where create_date between #{startYesterday} and #{endYesterDay} order by create_date desc limit 1
	</select>
</mapper>