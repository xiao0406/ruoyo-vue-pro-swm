<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.swm.dal.mysql.SwmBeaconStationMapper">
    
    <!-- 
        娉ㄦ剰锛氬熀鏈殑CRUD鎿嶄綔宸茬敱瀹炰綋绫绘敞瑙ｈ嚜鍔ㄥ鐞?
        姝ゆ枃浠朵腑鍙繚鐣欑壒娈婃煡璇㈡柟娉?
    -->

    <sql id="swmBeaconStationQueryColumns">
        a.id AS "id",
        a.beacon_id AS "beaconId",
        a.device_name AS "deviceName",
        a.beacon_type AS "beaconType",
        a.beacon_color AS "beaconColor",
        a.control_type AS "controlType",
        a.location AS "location",
        a.area AS "area",
        a.map_coord AS "mapCoord",
        a.pixel_x AS "pixelX",
        a.pixel_y AS "pixelY",
        a.real_x AS "realX",
        a.real_y AS "realY",
        a.gps_coord AS "gpsCoord",
        a.gps_longitude AS "gpsLongitude",
        a.gps_latitude AS "gpsLatitude",
        a.beacon_status AS "beaconStatus",
        a.deploy_status AS "deployStatus",
        a.stream_url AS "streamUrl",
        a.floor_id AS "floorId",
        a.building_id AS "buildingId",
        building_node.map_name AS "building",
        floor_node.map_name AS "floor",
        a.create_by AS "createBy",
        a.create_date AS "createDate",
        a.update_by AS "updateBy",
        a.update_date AS "updateDate",
        a.remarks AS "remarks",
        a.status AS "status",
        a.tenant_id AS "tenantId",
        a.tenant_name AS "tenantName"
    </sql>

    <!-- 鑾峰彇鍗曟潯淇℃爣锛屽苟琛ラ綈寤虹瓚/妤煎眰鍚嶇О -->
    <select id="get" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            <include refid="swmBeaconStationQueryColumns"/>
        FROM swm_beacon_station a
        LEFT JOIN swm_site_map_management building_node
            ON building_node.id = a.building_id
            <if test="tenantId != null and tenantId != ''">
                AND building_node.tenant_id = #{tenantId}
            </if>
        LEFT JOIN swm_site_map_management floor_node
            ON floor_node.id = a.floor_id
            <if test="tenantId != null and tenantId != ''">
                AND floor_node.tenant_id = #{tenantId}
            </if>
        WHERE a.id = #{id}
        <if test="tenantId != null and tenantId != ''">
            AND a.tenant_id = #{tenantId}
        </if>
    </select>

    <!-- 鏌ヨ淇℃爣鍒楄〃锛屽苟鎸夊缓绛戝悕绉般€佹ゼ灞侷D杩囨护 -->
    <select id="findList" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            <include refid="swmBeaconStationQueryColumns"/>
        FROM swm_beacon_station a
        LEFT JOIN swm_site_map_management building_node
            ON building_node.id = a.building_id
            <if test="tenantId != null and tenantId != ''">
                AND building_node.tenant_id = #{tenantId}
            </if>
        LEFT JOIN swm_site_map_management floor_node
            ON floor_node.id = a.floor_id
            <if test="tenantId != null and tenantId != ''">
                AND floor_node.tenant_id = #{tenantId}
            </if>
        <where>
            a.status = '0'
            <if test="tenantId != null and tenantId != ''">
                AND a.tenant_id = #{tenantId}
            </if>
            <if test="beaconId != null and beaconId != ''">
                AND a.beacon_id LIKE CONCAT('%', #{beaconId}, '%')
            </if>
            <if test="deviceName != null and deviceName != ''">
                AND a.device_name LIKE CONCAT('%', #{deviceName}, '%')
            </if>
            <if test="beaconType != null and beaconType != ''">
                AND a.beacon_type = #{beaconType}
            </if>
            <if test="location != null and location != ''">
                AND a.location LIKE CONCAT('%', #{location}, '%')
            </if>
            <if test="area != null and area != ''">
                AND a.area LIKE CONCAT('%', #{area}, '%')
            </if>
            <if test="beaconStatus != null and beaconStatus != ''">
                AND a.beacon_status = #{beaconStatus}
            </if>
            <if test="deployStatus != null and deployStatus != ''">
                AND a.deploy_status = #{deployStatus}
            </if>
            <if test="buildingId != null and buildingId != ''">
                AND a.building_id = #{buildingId}
            </if>
            <if test="building != null and building != ''">
                AND building_node.map_name LIKE CONCAT('%', #{building}, '%')
            </if>
            <if test="floorId != null and floorId != ''">
                AND a.floor_id = #{floorId}
            </if>
        </where>
        ORDER BY 
    </select>

    <!-- 鏍规嵁淇℃爣缂栧彿鏌ヨ鍩虹珯 -->
    <select id="getByBeaconId" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.beacon_id = #{beaconId}
        LIMIT 1
    </select>
    
    <!-- 鏍规嵁淇℃爣缂栧彿鍒楄〃鎵归噺鏌ヨ鍩虹珯 -->
    <select id="findByBeaconIds" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.beacon_id IN 
        <foreach collection="beaconIds" item="beaconId" open="(" separator="," close=")">
            #{beaconId}
        </foreach>
    </select>
    
    <!-- 妫€鏌ユ槸鍚﹀瓨鍦ㄧ浉鍚屼俊鏍囩紪鍙蜂笖鐘舵€佷负姝ｅ父鐨勮褰曪紙鐢ㄤ簬閲嶅鎬ф牎楠岋級 -->
    <select id="countByBeaconIdAndStatus" resultType="int">
        SELECT COUNT(1)
        FROM swm_beacon_station a
        WHERE a.beacon_id = #{beaconId}
        AND a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        <if test="id != null and id != ''">
            AND a.id != #{id} <!-- 鎺掗櫎褰撳墠璁板綍ID锛岀敤浜庣紪杈戞椂鏍￠獙 -->
        </if>
    </select>
    
    <!-- 鏌ヨ鎸囧畾浣嶇疆鐨勫熀绔欏垪琛?-->
    <select id="findByLocation" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.location LIKE CONCAT('%', #{location}, '%')
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鏌ヨ鍦ㄧ嚎鐘舵€佺殑鍩虹珯鍒楄〃 -->
    <select id="findOnlineBeacons" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.beacon_status = '1' <!-- BeaconStatusEnum.ONLINE -->
        AND a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鏍规嵁GPS鍧愭爣鑼冨洿鏌ヨ鍩虹珯鍒楄〃 -->
    <select id="findByGpsRange" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        <if test="minLongitude != null">
            AND a.gps_longitude >= #{minLongitude}
        </if>
        <if test="maxLongitude != null">
            AND a.gps_longitude &lt;= #{maxLongitude}
        </if>
        <if test="minLatitude != null">
            AND a.gps_latitude >= #{minLatitude}
        </if>
        <if test="maxLatitude != null">
            AND a.gps_latitude &lt;= #{maxLatitude}
        </if>
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鏍规嵁鍥剧焊鍍忕礌鍧愭爣鑼冨洿鏌ヨ鍩虹珯鍒楄〃 -->
    <select id="findByPixelRange" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        <if test="minX != null">
            AND a.pixel_x >= #{minX}
        </if>
        <if test="maxX != null">
            AND a.pixel_x &lt;= #{maxX}
        </if>
        <if test="minY != null">
            AND a.pixel_y >= #{minY}
        </if>
        <if test="maxY != null">
            AND a.pixel_y &lt;= #{maxY}
        </if>
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鏍规嵁鍖哄煙鏌ヨ鍩虹珯鍒楄〃 -->
    <select id="findByArea" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.area = #{area}
        AND a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鑾峰彇鎵€鏈夊尯鍩熷垪琛?-->
    <select id="findAllAreas" resultType="java.lang.String">
        SELECT DISTINCT a.area
        FROM swm_beacon_station a
        WHERE a.area IS NOT NULL 
        AND a.area != ''
        AND a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        ORDER BY a.area ASC
    </select>

    <!-- 鑾峰彇褰撳墠淇℃爣鍒楄〃瀹為檯浣跨敤鍒扮殑鍖哄煙ID -->
    <select id="findAreaOptionsFromBeacon" resultType="java.lang.String">
        <!-- 杩欓噷澶嶇敤鍒楄〃鐨勬暟鎹寖鍥存潯浠讹紝淇濊瘉鍜屽綋鍓嶄俊鏍囧垪琛ㄥ彛寰勪竴鑷淬€?-->
        SELECT DISTINCT TRIM(a.area)
        FROM swm_beacon_station a
        <where>
            a.status = '0'
            <if test="tenantId != null and tenantId != ''">
                AND a.tenant_id = #{tenantId}
            </if>
            AND a.area IS NOT NULL
            AND TRIM(a.area) != ''
        </where>
        ORDER BY TRIM(a.area) ASC
    </select>

    <!-- 鏍规嵁鍖哄煙ID鍒楄〃鎵归噺鏌ヨ鍖哄煙鍚嶇О -->
    <select id="findAreaNameMappings" resultType="java.util.HashMap">
        SELECT
            a.id AS "value",
            a.area_name AS "label"
        FROM swm_area a
        WHERE a.id IN
        <foreach collection="areaIds" item="areaId" open="(" separator="," close=")">
            #{areaId}
        </foreach>
        <if test="tenantId != null and tenantId != ''">
            AND a.tenant_id = #{tenantId}
        </if>
    </select>

    <!-- 鑾峰彇褰撳墠淇℃爣鍒楄〃瀹為檯浣跨敤鍒扮殑妤煎眰ID -->
    <select id="findFloorOptionsFromBeacon" resultType="java.lang.String">
        SELECT DISTINCT TRIM(a.floor_id)
        FROM swm_beacon_station a
        <where>
            a.status = '0'
            <if test="tenantId != null and tenantId != ''">
                AND a.tenant_id = #{tenantId}
            </if>
            AND a.floor_id IS NOT NULL
            AND TRIM(a.floor_id) != ''
        </where>
        ORDER BY TRIM(a.floor_id) ASC
    </select>

    <!-- 鏍规嵁妤煎眰ID鍒楄〃鎵归噺鏌ヨ妤煎眰鍚嶇О -->
    <select id="findFloorNameMappings" resultType="java.util.HashMap">
        SELECT
            a.id AS "value",
            a.map_name AS "label"
        FROM swm_site_map_management a
        WHERE a.map_type = 'floor'
        AND a.id IN
        <foreach collection="floorIds" item="floorId" open="(" separator="," close=")">
            #{floorId}
        </foreach>
        <if test="tenantId != null and tenantId != ''">
            AND a.tenant_id = #{tenantId}
        </if>
    </select>

    <!-- 鏍规嵁绮剧‘鐨勫儚绱犲潗鏍囨煡鎵句俊鏍?-->
    <select id="findByPixelCoordinates" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT 
            *
        FROM swm_beacon_station a
        WHERE a.pixel_x = #{pixelX}
        AND a.pixel_y = #{pixelY}
        AND a.status = '0' <!-- 姝ｅ父鐘舵€?-->
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 娓呯┖鎸囧畾鍖哄煙涓嬫墍鏈変俊鏍囩殑鍖哄煙鍜岄鑹插瓧娈?-->
    <update id="clearAreaAndColorByArea">
        UPDATE swm_beacon_station 
        SET area = NULL, beacon_color = NULL, update_by = #{updateBy}, update_date = #{updateDate}
        WHERE area = #{area} AND status = '0'
    </update>
    
    <!-- 鎵归噺娓呯┖鎸囧畾淇℃爣鐨勫尯鍩熷拰棰滆壊瀛楁 -->
    <update id="clearAreaAndColorByIds">
        UPDATE swm_beacon_station
        SET area = NULL, beacon_color = NULL, update_by = #{updateBy}, update_date = #{updateDate}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND status = '0'
    </update>
    <update id="updateBatch">
        <foreach collection="list" item="item" separator=";">
            UPDATE swm_beacon_station
            <set>
                <if test="item.major != null">
                    major = #{item.major},
                </if>
                <if test="item.minor != null">
                    minor = #{item.minor},
                </if>
                update_date = NOW()
            </set>
            WHERE beacon_id = #{item.beaconId}
        </foreach>
    </update>

    <!-- 鏌ヨ娌℃湁鍖哄煙ID鐨勪俊鏍囧垪琛?-->
    <select id="findBeaconsWithoutArea" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            *
        FROM swm_beacon_station a
        WHERE (a.area IS NULL OR a.area = '' OR TRIM(a.area) = '')
        AND a.status = '0'
        ORDER BY a.update_date DESC
    </select>

    <!-- 鏌ヨ鍗遍櫓婧愮被鍨嬬殑淇℃爣鍒楄〃 -->
    <select id="findDangerousSourceBeacons" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            *
        FROM swm_beacon_station a
        WHERE a.status = '0' 
        AND a.beacon_type = '3'  <!-- 鍗遍櫓婧愮被鍨?-->
        ORDER BY a.update_date DESC
    </select>

    <!-- 鏌ヨ鍙敤鐨勫嵄闄╂簮绫诲瀷淇℃爣鍒楄〃锛堟帓闄ゅ凡琚娇鐢ㄧ殑锛?-->
    <select id="findAvailableDangerousSourceBeacons" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            *
        FROM swm_beacon_station a
        WHERE a.status = '0' 
        AND a.beacon_type = '3'  <!-- 鍗遍櫓婧愮被鍨?-->
        AND NOT EXISTS (
            SELECT 1 FROM swm_hazard_source h
            WHERE h.status = '0'
            <if test="excludeHazardSourceId != null and excludeHazardSourceId != ''">
                AND h.id != #{excludeHazardSourceId}  <!-- 鎺掗櫎褰撳墠缂栬緫鐨勮褰?-->
            </if>
            AND (
                h.beacon_identifier = a.beacon_id
                OR h.beacon_identifier LIKE CONCAT(a.beacon_id, ',%')
                OR h.beacon_identifier LIKE CONCAT('%,', a.beacon_id, ',%')
                OR h.beacon_identifier LIKE CONCAT('%,', a.beacon_id)
            )
        )
        ORDER BY a.update_date DESC
    </select>
    <select id="findMacList" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmBeaconStationDO">
        SELECT
            *
        FROM swm_beacon_station a
        WHERE a.status = '0'
          AND #{random} = #{random}
    </select>
    <select id="findBeaconLocationForMap" resultType="java.util.Map">
        SELECT
            beacon_id,
            location,
            pixel_x,
            pixel_y
        FROM swm_beacon_station
        WHERE beacon_type = '1'
          AND `status` = 0
          AND beacon_id IS NOT NULL
          AND pixel_x IS NOT NULL
          AND pixel_y IS NOT NULL
    </select>

</mapper>
