<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.swm.dal.mysql.SwmHelmetDeviceMapper">
    
    <!-- 
        娉ㄦ剰锛氬熀鏈殑CRUD鎿嶄綔宸茬敱瀹炰綋绫绘敞瑙ｈ嚜鍔ㄥ鐞?
        姝ゆ枃浠朵腑鍙繚鐣欑壒娈婃煡璇㈡柟娉?
    -->
    
    <!-- 鏌ヨ鍙敤鐨勫畨鍏ㄥ附鍒楄〃锛堟湭缁戝畾浜哄憳鐨勶級 -->
    <select id="findAvailableHelmets" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
            a.id,
            a.device_id,
            a.helmet_type,
            a.battery_level,
            a.ip,
            a.mac_address,
            a.assigned_person,
            a.person_name,
            a.person_phone,
            a.assigned_workshop,
            a.assigned_process,
            a.assigned_team,
            a.motion_status,
            a.bind_time,
            a.unbind_time,
            a.bind_duration_days,
            a.usage_status,
            a.server_ip,
            a.server_port,
            a.bluetooth_scan_window,
            a.group_duration,
            a.normal_beacon_cs,
            a.special_beacon_cs,
            a.location_mode,
            a.deep_sleep_duration,
            a.bluetooth_scan_duration,
            a.send_interval,
            a.hazard_retrigger_interval,
            a.sleep_wakeup_time,
            a.beacon_filter_name,
            a.create_by,
            a.create_date,
            a.update_by,
            a.update_date,
            a.remarks,
            a.imei,
            a.device_color
        FROM swm_helmet_device a
        WHERE a.status = '0'
        AND (a.assigned_person IS NULL OR a.assigned_person = '')
        <if test="keyword != null and keyword != ''">
            AND a.device_id LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 鏍规嵁澶寸洈缂栧彿鏌ヨ璁惧 -->
    <select id="getByDeviceId" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
            a.*
        FROM swm_helmet_device a
        WHERE a.device_id = #{deviceId}
        AND a.status = '0'
        LIMIT 1
    </select>
    
    <!-- 寮哄埗娓呯┖璁惧缁戝畾淇℃伅锛堝皢assigned_person绛夊瓧娈佃缃负null锛?-->
    <update id="clearDeviceAssignment">
        UPDATE swm_helmet_device 
        SET 
            assigned_person = NULL,
            person_name = NULL,
            person_phone = NULL,
            assigned_workshop = NULL,
            assigned_process = NULL,
            assigned_team = NULL,
            unbind_time = NOW(),
            usage_status = 0,
            update_date = NOW()
        WHERE device_id = #{deviceId}
        AND status = '0'
    </update>
    
    <!-- 甯︽湁浜哄憳鍏宠仈淇℃伅鐨勫垪琛ㄦ煡璇紝鐢ㄤ簬鏇夸唬瀹炰綋绫婚粯璁ゆ煡璇?-->
    <!-- 娉ㄦ剰锛欽eeSite妗嗘灦浼氳嚜鍔ㄥ湪SQL鏈熬娣诲姞LIMIT瀛愬彞锛屾棤闇€鎵嬪姩娣诲姞 -->
    <select id="findHelmetDeviceListWithRelations" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
            a.id,
            a.device_id,
            a.helmet_type,
            a.battery_level,
            a.ip,
            a.mac_address,
            a.assigned_person,
            sp.name as personName,
            sp.phone_number as personPhone,
            fpa.position_name AS assigned_workshop,
            wg.work_group_name as assigned_team,
            a.motion_status,
            a.bind_time,
            a.unbind_time,
            a.bind_duration_days,
            a.usage_status,
            a.server_ip,
            a.server_port,
            a.bluetooth_scan_window,
            a.group_duration,
            a.normal_beacon_cs,
            a.special_beacon_cs,
            a.location_mode,
            a.deep_sleep_duration,
            a.bluetooth_scan_duration,
            a.send_interval,
            a.hazard_retrigger_interval,
            a.sleep_wakeup_time,
            a.beacon_filter_name,
            a.create_by,
            a.create_date,
            a.update_by,
            a.update_date,
            a.remarks,
            a.imei,
            a.device_color
        FROM swm_helmet_device a
        LEFT JOIN swm_person sp ON a.assigned_person = sp.identity_card COLLATE utf8mb4_general_ci and sp.`status` = '0'
        LEFT JOIN fms_position_archive fpa  ON sp.department = fpa.id COLLATE utf8mb4_general_ci
        LEFT JOIN fms_work_group wg ON sp.team = wg.id COLLATE utf8mb4_general_ci
        <where>
            a.status = '0' 
            <if test="deviceId != null and deviceId != ''">
                AND a.device_id LIKE CONCAT('%', #{deviceId}, '%')
            </if>
            <if test="helmetType != null and helmetType != ''">
                AND a.helmet_type = #{helmetType}
            </if>
            <if test="assignedPerson != null and assignedPerson != ''">
                AND a.assigned_person LIKE CONCAT('%', #{assignedPerson}, '%')
            </if>
            <if test="personName != null and personName != ''">
                AND a.person_name LIKE CONCAT('%', #{personName}, '%')
            </if>
            <if test="imei != null and imei != ''">
                AND a.imei LIKE CONCAT('%', #{imei}, '%')
            </if>
            <if test="deviceColor != null and deviceColor != ''">
                AND a.device_color LIKE CONCAT('%', #{deviceColor}, '%')
            </if>

            <!-- 鍙湁鍦?powerOnStatus 涓嶄负绌烘椂鎵嶅姞鏉′欢 -->
            <if test="powerOnStatus != null and powerOnStatus != ''">
                <!-- 鍋囧 status = 0锛屽垯浣跨敤 IN -->
                <if test="powerOnStatus == '0'.toString() ">
                    AND a.device_id IN
                    <foreach collection="deviceOnlist" item="id" open="(" separator="," close=")">
                        #{id}
                    </foreach>
                </if>
                <!-- 鍚﹀垯 NOT IN -->
                <if test="powerOnStatus == '1'.toString() ">
                    AND a.device_id NOT IN
                    <foreach collection="deviceOnlist" item="id" open="(" separator="," close=")">
                        #{id}
                    </foreach>
                </if>
            </if>


        </where>
        group by a.id
        ORDER BY a.update_date DESC
    </select>
    
    <!-- 甯︽湁浜哄憳鍏宠仈淇℃伅鐨勮鏁版煡璇紝鐢ㄤ簬鏇夸唬妗嗘灦榛樿鐨勮鏁版煡璇?-->
    <select id="findHelmetDeviceCountWithRelations" resultType="Long">
        SELECT COALESCE(COUNT(DISTINCT a.id), 0)
        FROM swm_helmet_device a
        LEFT JOIN fms_worker w ON a.assigned_person = w.identity_code COLLATE utf8mb4_general_ci
        LEFT JOIN fms_work_group wg ON w.work_group_id = wg.id COLLATE utf8mb4_general_ci
        LEFT JOIN fms_position_archive pa ON w.work_shop_code = pa.id COLLATE utf8mb4_general_ci
        <where>
            a.status = '0' 
            <if test="deviceId != null and deviceId != ''">
                AND a.device_id LIKE CONCAT('%', #{deviceId}, '%')
            </if>
            <if test="helmetType != null and helmetType != ''">
                AND a.helmet_type = #{helmetType}
            </if>
            <if test="assignedPerson != null and assignedPerson != ''">
                AND a.assigned_person LIKE CONCAT('%', #{assignedPerson}, '%')
            </if>
            <if test="personName != null and personName != ''">
                AND a.person_name LIKE CONCAT('%', #{personName}, '%')
            </if>
        </where>
    </select>

    <!-- 鏍规嵁璁惧ID鍒楄〃鏌ヨ璁惧璇︽儏锛堝甫浜哄憳鍏宠仈淇℃伅锛?-->
    <select id="findHelmetDeviceListByDeviceIds" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
            a.id,
            a.device_id,
            a.helmet_type,
            a.battery_level,
            a.ip,
            a.mac_address,
            a.assigned_person,
            a.person_name,
            a.person_phone,
            CASE
                WHEN a.assigned_person IS NOT NULL AND a.assigned_person != '' THEN pa.position_name
                ELSE a.assigned_workshop
            END AS assigned_workshop,
            a.assigned_process,
            CASE
                WHEN a.assigned_person IS NOT NULL AND a.assigned_person != '' THEN wg.work_group_name
                ELSE a.assigned_team
            END AS assigned_team,
            a.motion_status,
            a.bind_time,
            a.unbind_time,
            a.bind_duration_days,
            a.usage_status,
            a.server_ip,
            a.server_port,
            a.bluetooth_scan_window,
            a.group_duration,
            a.normal_beacon_cs,
            a.special_beacon_cs,
            a.location_mode,
            a.deep_sleep_duration,
            a.bluetooth_scan_duration,
            a.send_interval,
            a.hazard_retrigger_interval,
            a.sleep_wakeup_time,
            a.beacon_filter_name,
            a.create_by,
            a.create_date,
            a.update_by,
            a.update_date,
            a.remarks,
            a.imei,
            a.device_color
        FROM swm_helmet_device a
        LEFT JOIN fms_worker w ON a.assigned_person = w.identity_code COLLATE utf8mb4_general_ci
        LEFT JOIN fms_work_group wg ON w.work_group_id = wg.id COLLATE utf8mb4_general_ci
        LEFT JOIN fms_position_archive pa ON w.work_shop_code = pa.id COLLATE utf8mb4_general_ci
        <where>
            a.status = '0' 
            <if test="deviceIdList != null and deviceIdList.size() > 0">
                AND a.device_id IN
                <foreach collection="deviceIdList" item="deviceId" open="(" separator="," close=")">
                    #{deviceId}
                </foreach>
            </if>
            <if test="helmetType != null and helmetType != ''">
                AND a.helmet_type = #{helmetType}
            </if>
            <if test="assignedPerson != null and assignedPerson != ''">
                AND a.assigned_person LIKE CONCAT('%', #{assignedPerson}, '%')
            </if>
            <if test="personName != null and personName != ''">
                AND a.person_name LIKE CONCAT('%', #{personName}, '%')
            </if>
        </where>
        ORDER BY a.update_date DESC
    </select>
    <select id="findDeviceTenantMapping" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
            device_id, tenant_id,assigned_person
        FROM
            swm_helmet_device a
        where `status` = '0'
          AND tenant_id = #{tenantId}
          AND #{random} = #{random}
    </select>
    <select id="findListInit" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        select * from swm_helmet_device
        <where>
            `status` = '0'
            <if test="random != null and random != 0 ">
                AND #{random} = #{random}
            </if>
        </where>
    </select>
    <!-- 鎵归噺鏍规嵁璁惧ID锛堜富閿甶d锛夋煡璇㈠畨鍏ㄥ附璁惧鍒楄〃 -->
    <select id="findHelmetDeviceListByIds" parameterType="java.util.List" resultType="cn.iocoder.yudao.module.swm.dal.dataobject.SwmHelmetDeviceDO">
        SELECT
        a.*
        FROM
        swm_helmet_device a
        WHERE
        a.id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND a.status = '0'
    </select>
    <select id="findDeviceIdsByDeviceSource" resultType="java.lang.String">
        SELECT
        a.device_id
        FROM
        swm_helmet_device a
        WHERE
        a.device_source = #{deviceSource}
        AND a.status = '0'
        AND #{random} = #{random}
    </select>
    <select id="selectCount" resultType="java.lang.Long">
        SELECT
        COUNT(a.id)
        FROM swm_helmet_device a
        <where>
            a.status = '0' 
            <if test="deviceId != null and deviceId != ''">
                AND a.device_id LIKE CONCAT('%', #{deviceId}, '%')
            </if>
            <if test="helmetType != null and helmetType != ''">
                AND a.helmet_type = #{helmetType}
            </if>
            <if test="assignedPerson != null and assignedPerson != ''">
                AND a.assigned_person LIKE CONCAT('%', #{assignedPerson}, '%')
            </if>
            <if test="personName != null and personName != ''">
                AND a.person_name LIKE CONCAT('%', #{personName}, '%')
            </if>
            <if test="imei != null and imei != ''">
                AND a.imei LIKE CONCAT('%', #{imei}, '%')
            </if>
            <if test="deviceColor != null and deviceColor != ''">
                AND a.device_color LIKE CONCAT('%', #{deviceColor}, '%')
            </if>

            <!-- 寮€鏈虹姸鎬佹潯浠?-->
            <if test="powerOnStatus != null and powerOnStatus != ''">
                <if test="powerOnStatus == '0'.toString() ">
                    AND a.device_id IN
                    <foreach collection="deviceOnlist" item="id" open="(" separator="," close=")">
                        #{id}
                    </foreach>
                </if>
                <if test="powerOnStatus == '1'.toString() ">
                    AND a.device_id NOT IN
                    <foreach collection="deviceOnlist" item="id" open="(" separator="," close=")">
                        #{id}
                    </foreach>
                </if>
            </if>
        </where>
    </select>

</mapper> 
